import { LoaderCircleIcon } from 'lucide-react';

const LoadingIcon = () => {
  return <LoaderCircleIcon className="inline size-[1em] animate-spin" aria-label="Loading" />;
};

export default LoadingIcon;
