// Enum of all of the table components so we can store different settings for different tables

export enum TableKey {
  AllCensuses,
  AllLanguages,
  AllLocales,
  AllTerritories,
  AllVariantTags,
  AllWritingSystems,
  LanguagesInCensus,
  LanguagesInTerritory,
}
